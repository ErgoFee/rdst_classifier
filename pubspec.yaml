name: rdst_classifier
description: "Pure Dart inference engine for the Random Dilated Shapelet Transform (RDST) classifier. Loads pre-trained models exported from Python/aeon and runs inference."
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.3 <4.0.0'

dev_dependencies:
  archive: ^3.6.1
  lints: ^3.0.0
  test: ^1.24.0
